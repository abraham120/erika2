component.mk is used for the Altera makefiles. the component.mk file
is included inside the typical makefiles. Evidence makefiles do not
use component.mk.

To compile a binary distribution, we still use the typical cfg.mk
